# CloudGuard Pro CSPM v3 — Python Requirements
# Aniza Corp | Shahryar Jahangir
#
# Python 3.14 note:
#   pydantic-core 2.27+ uses pyo3 0.22.5+ which fully supports Python 3.14.
#   Do NOT downgrade pydantic-core below 2.27 on Python 3.14.

# ─── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.111.1,<0.120.0
uvicorn[standard]>=0.30.1,<0.35.0
python-multipart>=0.0.9

# ─── Database / ORM ──────────────────────────────────────────────────────────
sqlalchemy>=2.0.31,<2.1.0
alembic>=1.13.2,<2.0.0

# ─── Validation / serialization ───────────────────────────────────────────────
# pydantic-core 2.27+ uses pyo3 0.22.5 — first version with Python 3.14 support.
# This must be pinned explicitly; pydantic alone will not force a high enough
# pydantic-core version on fresh resolves with an empty pip cache.
pydantic-core>=2.27.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# ─── PDF generation ──────────────────────────────────────────────────────────
reportlab>=4.2.2

# ─── YAML ────────────────────────────────────────────────────────────────────
pyyaml>=6.0.1

# ─── Cloud SDKs ───────────────────────────────────────────────────────────────
# AWS (enabled by default)
boto3>=1.34.144
botocore>=1.34.144

# Azure — uncomment to enable:
# azure-identity>=1.17.1
# azure-mgmt-resource>=23.1.1
# azure-mgmt-storage>=21.1.0
# azure-mgmt-sql>=3.0.1
# azure-mgmt-keyvault>=10.3.1
# azure-mgmt-network>=25.4.0
# azure-mgmt-containerservice>=31.0.0
# azure-mgmt-compute>=31.0.0
# azure-mgmt-monitor>=6.0.2
# azure-mgmt-security>=7.0.0

# GCP — uncomment to enable:
# google-cloud-storage>=2.17.0
# google-cloud-kms>=2.24.0
# google-cloud-container>=2.44.0
# google-cloud-bigquery>=3.25.0
# google-auth>=2.32.0
# google-api-python-client>=2.139.0

# IBM Cloud — uncomment to enable:
# ibm-cloud-sdk-core>=3.20.6
# ibm-platform-services>=0.53.2

# OCI — uncomment to enable:
# oci>=2.129.0

# ─── Dev / testing ────────────────────────────────────────────────────────────
pytest>=8.3.1
pytest-asyncio>=0.23.7
httpx>=0.27.0
pytest-cov>=5.0.0

# ─── Linting / typing ─────────────────────────────────────────────────────────
ruff>=0.5.2
mypy>=1.10.1
